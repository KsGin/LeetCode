#include <string>
#include <vector>

using std::string;
using std::vector;

class NQueens {
public:
    vector<vector<string>> solveNQueens(int n) {
        vector<vector<string>> ret(0);

        return ret;
    }
};